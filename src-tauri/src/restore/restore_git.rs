//! Restores Git configuration (~/.gitconfig) and shell configs.
//! Implemented in Milestone 8.

use anyhow::Result;

pub async fn restore_git_config() -> Result<()> {
    // Milestone 8: write ~/.gitconfig from snapshot.git.gitconfig.
    Ok(())
}
