//! Restores Python and pip packages.
//! Implemented in Milestone 8.

use anyhow::Result;

pub async fn restore_python() -> Result<()> {
    // Milestone 8: install python via brew, `pip3 install <pkg>` per package.
    Ok(())
}
