//! Restores Node.js and global npm packages.
//! Implemented in Milestone 8.

use anyhow::Result;

pub async fn restore_node() -> Result<()> {
    // Milestone 8: install nvm/node via brew, `npm install -g <pkg>` per package.
    Ok(())
}
